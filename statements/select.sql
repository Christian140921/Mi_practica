--Esto es una consulta simple a una tabla de una bd
SELECT * FROM usuarios; 
--Esto es una consulta que selecciona columnas específicas
SELECT user_id, name FROM usuarios;
--Nuevamente es una consulta simple
SELECT * FROM pedidos;
--Nuevamente es una consulta que selecciona columnas específicas
SELECT producto FROM pedidos